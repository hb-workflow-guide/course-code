name: First Workflow 
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello Family"
      - name: Multiline command
        run: |
          node -v
          npm -v
